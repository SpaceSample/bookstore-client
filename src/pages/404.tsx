import Box from '@mui/material/Box';
import Typography from '@mui/material/Typography';

const NotFund = () => {
  return (
    <Box sx={{ textAlign: 'center', color: '#b83f45', m: '100px 0' }}>
      <Typography variant="h1" gutterBottom>
        404 Not Found
      </Typography>
    </Box>
  );
};

export default NotFund;
